# ve-designsys

Crisis Group's midlevel explainer design system.

Open `index.html` in a browser to review the current static sample page. The page uses dummy media blocks to show
expected aspect ratios, crop scope and interaction patterns without requiring production assets.
